@{
    ViewBag.Title = "Historic Records";

    //check admin privileges
    var adminRole = ELG.Web.Helper.SessionHelper.UserRole;
    Layout = ELG.Web.Helper.CommonHelper.GetAdminLayoutPage(adminRole);
}

<div class="hr-modern-page">
    <header class="hr-hero">
        <div>
            <nav class="hr-breadcrumb" aria-label="breadcrumb">
                <a href="@Url.Action("InsightsReports", "Report")">Reports &amp; Analytics</a>
                <span>/</span>
                <span>Historic Records</span>
            </nav>
            <h1 class="hr-title">Historic Records</h1>
            <p class="hr-subtitle">Review archived learner outcomes and certification history across courses.</p>
        </div>
        <a href="@Url.Action("InsightsReports", "Report")" class="hr-back-btn">
            <i class="fa fa-arrow-left"></i>
            Back to Reports
        </a>
    </header>

    <section class="hr-filter-card">
        <div class="row g-3 align-items-end">
            <div class="col-lg-6 col-md-6">
                <label class="form-label hr-label" for="txtLearner">User</label>
                <div class="input-group hr-input-group">
                    <span class="input-group-text"><i class="fa fa-search"></i></span>
                    <input type="text" class="form-control" id="txtLearner" placeholder="Name or email">
                </div>
            </div>

            <div class="col-lg-6 col-md-6">
                <label class="form-label hr-label" for="ddlCourse">Course</label>
                <select class="form-select ddl-course" id="ddlCourse">
                    <option value="0">Select All</option>
                </select>
            </div>
        </div>

        <div class="row g-3 align-items-end mt-1">
            <div class="col-lg-3 col-md-6">
                <label class="form-label hr-label" for="ddlStatus">Course Status</label>
                <select class="form-select" id="ddlStatus">
                    <option value="not-started">Not started</option>
                    <option value="incomplete">In-progress</option>
                    <option value="passed">Passed</option>
                    <option value="failed">Failed</option>
                    <option disabled>----------</option>
                    <option value="0" selected>Select All</option>
                    <option value="not-passed">Not Passed</option>
                </select>
            </div>

            <div class="col-lg-3 col-md-6">
                <label class="form-label hr-label" for="txtRptFrom">Completed From</label>
                <input type="text" class="form-control" id="txtRptFrom" placeholder="yyyy-mm-dd">
            </div>

            <div class="col-lg-3 col-md-6">
                <label class="form-label hr-label" for="txtRptTo">Completed To</label>
                <input type="text" class="form-control" id="txtRptTo" placeholder="yyyy-mm-dd">
            </div>

            <div class="col-lg-3 col-md-12">
                <div class="hr-actions-panel">
                    <button id="searchLearnerProgress" class="btn hr-btn hr-btn-primary">
                        <i class="fa fa-search me-1"></i>Search
                    </button>
                    <button id="downloadProgressReport" class="btn hr-btn hr-btn-soft"
                            data-loading-text="Downloading..."
                            data-original-text='<i class="fa fa-download"></i> Download'>
                        <i class="fa fa-download me-1"></i>Download
                    </button>
                    <button id="clearSearchProgressReport" class="btn hr-btn hr-btn-soft">
                        <i class="fa fa-refresh me-1"></i>Clear
                    </button>
                </div>
            </div>
        </div>
    </section>

    <section class="hr-table-card">
        <div class="hr-table-head">
            <h2>Historic Learning Records</h2>
            <span class="hr-table-hint">Use filters to find archived progress records and completion outcomes quickly.</span>
        </div>

        <div id="message_test_report" class="mb-3"></div>

        <div class="table-responsive" id="test-report-container">
            <table class="table table-hover align-middle" id="learningProgressReport" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>User</th>
                        <th>Email</th>
                        <th>@ELG.Web.Helper.SessionHelper.CompanySettings.strLocationDescription</th>
                        <th>@ELG.Web.Helper.SessionHelper.CompanySettings.strDepartmentDescription</th>
                        <th>Course</th>
                        <th>Assigned On</th>
                        <th>Last Accessed On</th>
                        <th>Status</th>
                        <th>Score</th>
                        <th>Completion Date</th>
                        <th>Moved to history on</th>
                        <th>Action</th>
                    </tr>
                </thead>
            </table>
        </div>
    </section>
</div>

<style>
    .hr-modern-page {
        --hr-surface: #ffffff;
        --hr-line: #d9e5ea;
        --hr-text: #12303a;
        --hr-muted: #6a808c;
        --hr-brand: var(--color-primary, #0b7d78);
        --hr-brand-soft: #e9f6f4;
        --hr-shadow: 0 8px 18px rgba(17, 39, 47, 0.06);
        color: var(--hr-text);
    }

    .hr-hero {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 1rem;
        padding: 0.2rem 0.2rem 0.65rem;
        margin-bottom: 0.45rem;
    }

    .hr-breadcrumb {
        display: flex;
        align-items: center;
        gap: 0.45rem;
        margin-bottom: 0.45rem;
        color: var(--hr-muted);
        font-size: 0.88rem;
    }

    .hr-breadcrumb a {
        color: var(--hr-muted);
        text-decoration: none;
    }

    .hr-breadcrumb a:hover {
        color: var(--hr-text);
        text-decoration: underline;
    }

    .hr-title {
        margin: 0;
        font-size: 2.1rem;
        font-weight: 800;
        letter-spacing: 0.2px;
        line-height: 1.15;
    }

    .hr-subtitle {
        margin: 0.3rem 0 0;
        color: #5d6f7a;
        font-size: 1.05rem;
        line-height: 1.35;
    }

    .hr-back-btn {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        border: 1px solid var(--hr-brand);
        color: var(--hr-brand);
        background: #fff;
        border-radius: 10px;
        padding: 0.45rem 0.75rem;
        font-size: 0.85rem;
        font-weight: 700;
        text-decoration: none;
        white-space: nowrap;
    }

    .hr-back-btn:hover {
        background: var(--hr-brand);
        color: #fff;
        text-decoration: none;
    }

    .hr-filter-card,
    .hr-table-card {
        background: var(--hr-surface);
        border: 1px solid var(--hr-line);
        border-radius: 14px;
        box-shadow: var(--hr-shadow);
    }

    .hr-filter-card {
        padding: 1rem 1rem 0.95rem;
        margin-bottom: 0.95rem;
    }

    .hr-label {
        font-size: 0.78rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.45px;
        color: #5f7884;
        margin-bottom: 0.38rem;
        line-height: 1.2;
    }

    .hr-input-group .input-group-text {
        border-color: #d2e3e8;
        background: #f4fbfa;
        color: #446573;
    }

    .hr-modern-page .form-control,
    .hr-modern-page .form-select {
        border-radius: 10px;
        border-color: #d2e3e8;
        box-shadow: none;
        min-height: 2.65rem;
    }

    .hr-modern-page .form-control:focus,
    .hr-modern-page .form-select:focus {
        border-color: #99c9c4;
        box-shadow: 0 0 0 0.16rem rgba(11, 125, 120, 0.12);
    }

    .hr-actions-panel {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 0.5rem;
    }

    .hr-btn {
        min-height: 2.65rem;
        border-radius: 10px;
        font-size: 0.9rem;
        font-weight: 700;
        line-height: 1.2;
    }

    .hr-btn-primary {
        background: var(--hr-brand);
        border-color: var(--hr-brand);
        color: #fff;
    }

    .hr-btn-primary:hover,
    .hr-btn-primary:focus {
        background: var(--hr-brand);
        border-color: var(--hr-brand);
        color: #fff;
        filter: brightness(0.94);
    }

    .hr-btn-soft {
        background: var(--hr-brand-soft);
        border-color: #cde4e2;
        color: #2a545f;
    }

    .hr-btn-soft:hover,
    .hr-btn-soft:focus {
        background: #dff2ef;
        border-color: #bdddd9;
        color: #1d4751;
    }

    .hr-table-card {
        padding: 0.85rem 1rem 1rem;
    }

    .hr-table-head {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 0.7rem;
        padding: 0.15rem 0.1rem 0.7rem;
    }

    .hr-table-head h2 {
        margin: 0;
        font-size: 1.1rem;
        font-weight: 800;
        color: #163641;
        line-height: 1.25;
    }

    .hr-table-hint {
        color: #68808c;
        font-size: 0.83rem;
    }

    #learningProgressReport thead th {
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.45px;
        color: #56717c;
        background: #f7fbfc;
        border-bottom: 1px solid #deebef;
        white-space: nowrap;
    }

    #learningProgressReport tbody td {
        font-size: 0.89rem;
        color: #1f3d47;
        vertical-align: middle;
        white-space: nowrap;
    }

    #learningProgressReport tbody tr:hover {
        background: #fbfefe;
    }

    @@media (max-width: 991px) {
        .hr-hero {
            flex-direction: column;
            align-items: stretch;
        }

        .hr-back-btn {
            align-self: flex-start;
        }

        .hr-table-head {
            flex-direction: column;
            align-items: flex-start;
        }
    }

    @@media (max-width: 767px) {
        .hr-title {
            font-size: 1.6rem;
        }

        .hr-filter-card,
        .hr-table-card {
            border-radius: 12px;
        }

        .hr-actions-panel {
            grid-template-columns: 1fr;
        }

        .hr-btn {
            width: 100%;
        }
    }
</style>

@section Scripts{
    <script type="text/javascript" src="~/Scripts/Admin/Reports/historicRecords.js?v=1.5"></script>
}
