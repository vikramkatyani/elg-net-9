<div class="modal fade" id="courseSubModuleModal" tabindex="-1" aria-labelledby="courseSubModuleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content cm-sub-content">
            <div class="modal-header cm-sub-header">
                <h5 class="modal-title cm-sub-title" id="courseSubModuleModalLabel">
                    <i class="fa fa-list"></i>
                    <span id="courseSubModuleTitle"></span>
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <div class="modal-body cm-sub-body">
                <div class="table-responsive">
                    <table class="table table-hover align-middle" id="courseSubModuleList" width="100%" cellspacing="0">
                        <thead>
                            <tr>
                                <th>Module</th>
                            </tr>
                        </thead>
                        <tbody>
                            <!-- Dynamic rows will be injected here -->
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="modal-footer cm-sub-footer">
                <button type="button" class="btn cm-sub-btn" data-bs-dismiss="modal">
                    <i class="fa fa-times"></i>Close
                </button>
            </div>
        </div>
    </div>
</div>

<style>
    #courseSubModuleModal .modal-dialog {
        max-width: 760px;
    }

    #courseSubModuleModal .cm-sub-content {
        border: 1px solid #d8e6ea;
        border-radius: 14px;
        box-shadow: 0 18px 36px rgba(14, 40, 46, 0.16);
        overflow: hidden;
        animation: cmSubPopupIn 0.2s ease-out;
    }

    #courseSubModuleModal .cm-sub-header {
        background: linear-gradient(145deg, #f0faf8, #eef5fb);
        border-bottom: 1px solid #dce9ec;
    }

    #courseSubModuleModal .cm-sub-title {
        display: inline-flex;
        align-items: center;
        gap: 0.45rem;
        color: #1f4652;
        font-weight: 700;
    }

    #courseSubModuleModal .cm-sub-body {
        background: #fcfefe;
        padding: 1rem;
    }

    #courseSubModuleModal table thead th {
        background: #f4faf9;
        border-bottom: 1px solid #dce9eb;
        color: #32515c;
        font-size: 0.82rem;
    }

    #courseSubModuleModal table tbody td {
        padding-top: 0.72rem;
        padding-bottom: 0.72rem;
    }

    #courseSubModuleModal .table-hover tbody tr:hover {
        background: #f5fbff;
    }

    #courseSubModuleModal .cm-sub-footer {
        background: #f9fcfd;
        border-top: 1px solid #dce9ec;
    }

    #courseSubModuleModal .cm-sub-btn {
        min-width: 110px;
        height: 38px;
        border-radius: 10px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.34rem;
        font-size: 0.82rem;
        font-weight: 700;
        background: #f7fbfd;
        border: 1px solid #d2e2e8;
        color: #2e515d;
        transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    #courseSubModuleModal .cm-sub-btn:hover {
        transform: translateY(-1px);
    }

    #courseSubModuleModal .cm-sub-btn:focus-visible {
        outline: none;
        box-shadow: 0 0 0 3px rgba(11, 125, 120, 0.24);
    }

    @@keyframes cmSubPopupIn {
        from {
            opacity: 0;
            transform: translateY(10px) scale(0.995);
        }

        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }

    @@media (prefers-reduced-motion: reduce) {
        #courseSubModuleModal .cm-sub-content {
            animation: none;
        }

        #courseSubModuleModal .cm-sub-btn {
            transition: none;
        }
    }

    @@media (max-width: 767px) {
        #courseSubModuleModal .modal-dialog {
            margin: 0.55rem;
        }

        #courseSubModuleModal .cm-sub-body {
            padding: 0.85rem;
        }

        #courseSubModuleModal .cm-sub-footer {
            display: grid;
            grid-template-columns: 1fr;
            gap: 0.5rem;
        }

        #courseSubModuleModal .cm-sub-btn {
            width: 100%;
        }
    }
</style>