
@{
    ViewBag.Title = "Reset Training";
    //check admin privileges
    var adminRole = ELG.Web.Helper.SessionHelper.UserRole;
    Layout = ELG.Web.Helper.CommonHelper.GetAdminLayoutPage(adminRole);
}

<div class="x_panel tile">
    <div class="x_title">
        <h1 class="h3 mb-3 text-primary fw-bold">Refresher Training</h1>

        <!-- Filter Section -->
        <div class="card mb-4">
            <div class="card-body">
                <div class="row g-3 align-items-end">

                    <!-- User Search -->
                    <div class="col-md-4 col-lg-3">
                        <div class="form-floating">
                            <input type="text" id="txtLearner" class="form-control" placeholder="Name/Email">
                            <label for="txtLearner">User</label>
                        </div>
                    </div>

                    <!-- Location Filter -->
                    <div class="col-md-4 col-lg-3">
                        <div class="form-floating">
                            <select id="ddlLocation" class="form-select ddl-loc">
                                <option value="0">Select All</option>
                            </select>
                            <label for="ddlLocation">@ELG.Web.Helper.SessionHelper.CompanySettings.strLocationDescription</label>
                        </div>
                    </div>

                    <!-- Department Filter -->
                    <div class="col-md-4 col-lg-3">
                        <div class="form-floating">
                            <select id="ddlDepartment" class="form-select ddl-dep">
                                <option value="0">Select All</option>
                            </select>
                            <label for="ddlDepartment">@ELG.Web.Helper.SessionHelper.CompanySettings.strDepartmentDescription</label>
                        </div>
                    </div>

                    <!-- Course Dropdown -->
                    <div class="col-md-4 col-lg-3">
                        <div class="form-floating">
                            <select id="ddlCourse" class="form-select ddl-course">
                                <option value="0">Select All</option>
                            </select>
                            <label for="ddlCourse">Course</label>
                        </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="col-12 d-flex justify-content-end gap-2 mt-3">
                        <button id="searchAllocatedModule" class="btn btn-primary">
                            <i class="fa fa-search me-1"></i> Search
                        </button>
                        <button id="clearSearchAllocateModule" class="btn btn-secondary">
                            <i class="fa fa-times me-1"></i> Clear
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Table Section -->
    <div class="x_content">
        <div id="divMessage_LearnerAssignedModule"></div>

        <div class="table-responsive" id="div-report-container">
            <div class="d-flex justify-content-end mb-2">
                <button type="button" class="btn btn-dark disabled" id="btnResetForSelected"
                        data-bs-toggle="tooltip" title="Select some records"
                        data-loading-text="Please wait..." data-original-text="Reset selected training records">
                    Reset Training
                </button>
            </div>

            <table class="table table-striped table-hover table-bordered align-middle" id="learnerAssignedModuleList" width="100%" cellspacing="0">
                <thead class="table-light">
                    <tr>
                        <th style="width: 5%;">
                            <input type="checkbox" name="chk_slc_all_user_rtm" id="chk_slc_all_user_rtm" value="1">
                            <span class="ms-1">Select All</span>
                        </th>
                        <th>User</th>
                        <th>Email</th>
                        <th>@ELG.Web.Helper.SessionHelper.CompanySettings.strLocationDescription</th>
                        <th>@ELG.Web.Helper.SessionHelper.CompanySettings.strDepartmentDescription</th>
                        <th>Course</th>
                        <th>Assigned On</th>
                        <th>Status</th>
                        <th class="text-center" style="width: 5%;">Action</th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>
</div>
@section Scripts{
    <script type="text/javascript" src="~/Scripts/Admin/CourseManagement/refreshTraining_Manual.js"></script>
}

