@{
    ViewBag.Title = "Documents";
    //check admin privileges
    var adminRole = ELG.Web.Helper.SessionHelper.UserRole;
    if (adminRole > 0)
    {
        Layout = ELG.Web.Helper.CommonHelper.GetAdminLayoutPage(adminRole);
    }
    else
    {
        <div class="alert alert-danger">Unauthorized access. Please <a href="@Url.Action("LogOut", "Account")">log out</a>.</div>
    }
}

<div class="dm-modern-page" id="documentsPage">
    <section class="dm-hero">
        <div>
            <h1 class="dm-title">Manage Documents</h1>
            <p class="dm-subtitle">Search, maintain, and lifecycle-manage uploaded documents across categories and sub-categories.</p>
        </div>
        <button id="btnShowDocPopUp" class="btn dm-btn dm-btn-success">
            <i class="fa fa-plus-circle"></i>Add Document
        </button>
    </section>

    <section class="dm-filter-card">
        <div class="dm-filter-head">
            <h2>Filters</h2>
            <p>Refine by name, category, sub-category, and status.</p>
        </div>

        <div class="row g-3 align-items-end">
            <div class="col-xl-3 col-lg-6 col-md-6 col-sm-12">
                <label class="form-label dm-label" for="txtDoc">Document</label>
                <div class="dm-control">
                    <span class="dm-control-icon"><i class="fa fa-file-text-o"></i></span>
                    <input type="text" id="txtDoc" class="form-control" placeholder="Search document name" autocomplete="off">
                </div>
            </div>

            <div class="col-xl-3 col-lg-6 col-md-6 col-sm-12">
                <label class="form-label dm-label" for="ddlCategory">Category</label>
                <div class="dm-control dm-control-select">
                    <span class="dm-control-icon"><i class="fa fa-folder-open-o"></i></span>
                    <select id="ddlCategory" class="form-select ddlCat">
                        <option value="0">Select</option>
                    </select>
                </div>
            </div>

            <div class="col-xl-3 col-lg-6 col-md-6 col-sm-12">
                <label class="form-label dm-label" for="ddlSubCategory">Sub Category</label>
                <div class="dm-control dm-control-select">
                    <span class="dm-control-icon"><i class="fa fa-folder-o"></i></span>
                    <select id="ddlSubCategory" class="form-select ddlSubCat">
                        <option value="0">Select</option>
                    </select>
                </div>
            </div>

            <div class="col-xl-3 col-lg-6 col-md-6 col-sm-12">
                <label class="form-label dm-label" for="ddlDocStatus">Status</label>
                <div class="dm-control dm-control-select">
                    <span class="dm-control-icon"><i class="fa fa-toggle-on"></i></span>
                    <select id="ddlDocStatus" class="form-select">
                        <option value="1" selected>Active</option>
                        <option value="0">Archived</option>
                        <option value="2">All</option>
                    </select>
                </div>
            </div>

            <div class="col-12">
                <div class="dm-actions">
                    <button id="searchDoc" class="btn dm-btn dm-btn-primary">
                        <i class="fa fa-search"></i>Search
                    </button>
                    <button id="clearSearchDoc" class="btn dm-btn dm-btn-soft">
                        <i class="fa fa-times"></i>Clear
                    </button>
                </div>
            </div>
        </div>
    </section>

    <section class="dm-table-card">
        <div class="dm-table-head">
            <h2>Documents</h2>
            <span class="dm-table-hint">Use actions to edit, preview, archive, activate, or delete a document.</span>
        </div>

        <div id="messageDocument" class="mb-3"></div>

        <div class="table-responsive" id="div-report-container">
            <table class="table table-hover align-middle" id="orgDocumentList" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Category</th>
                        <th>Sub Category</th>
                        <th>Version Number</th>
                        <th>Date Published</th>
                        <th>Date of Review</th>
                        <th style="width: 5%;">Action</th>
                    </tr>
                </thead>
            </table>
        </div>
    </section>
</div>

@Html.Partial("_addDocument")

<style>
    .dm-modern-page {
        --dm-surface: #ffffff;
        --dm-line: #d9e6ea;
        --dm-text: #14323c;
        --dm-muted: #627f8a;
        --dm-brand: var(--color-primary, #0b7d78);
        --dm-brand-alt: var(--color-secondary, #0f968f);
        --dm-brand-soft: #e8f5f3;
        --dm-shadow: 0 14px 30px rgba(17, 42, 48, 0.08);
        color: var(--dm-text);
        direction: ltr;
    }

    .dm-modern-page *,
    .dm-modern-page *::before,
    .dm-modern-page *::after {
        box-sizing: border-box;
    }

    .dm-hero {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        flex-wrap: wrap;
        gap: 1rem;
        background: linear-gradient(145deg, #effaf8, #edf6fb);
        border: 1px solid #d8e9ec;
        border-radius: 13px;
        padding: 1rem 1.15rem;
        margin-bottom: 1rem;
    }

    .dm-title {
        margin: 0;
        font-size: 1.5rem;
        font-weight: 800;
        letter-spacing: 0.2px;
    }

    .dm-subtitle {
        margin: 0.28rem 0 0;
        font-size: 0.92rem;
        line-height: 1.45;
        color: var(--dm-muted);
    }

    .dm-filter-card,
    .dm-table-card {
        background: var(--dm-surface);
        border: 1px solid var(--dm-line);
        border-radius: 14px;
        box-shadow: var(--dm-shadow);
    }

    .dm-filter-card {
        padding: 1rem;
        margin-bottom: 1rem;
    }

    .dm-filter-head {
        display: flex;
        justify-content: space-between;
        align-items: baseline;
        flex-wrap: wrap;
        gap: 0.8rem;
        margin-bottom: 0.8rem;
    }

    .dm-filter-head h2 {
        margin: 0;
        font-size: 1.02rem;
        font-weight: 700;
    }

    .dm-filter-head p {
        margin: 0;
        font-size: 0.8rem;
        color: var(--dm-muted);
    }

    .dm-label {
        font-size: 0.76rem;
        text-transform: uppercase;
        letter-spacing: 0.04em;
        color: #56707b;
        margin-bottom: 0.3rem;
    }

    .dm-control {
        display: flex;
        align-items: stretch;
        width: 100%;
        min-width: 0;
        border: 1px solid #cfe0e4;
        border-radius: 10px;
        background: #fff;
        overflow: hidden;
    }

    .dm-control:hover {
        border-color: #bad6dc;
    }

    .dm-control:focus-within {
        border-color: #90c8c4;
        box-shadow: 0 0 0 0.2rem rgba(11, 125, 120, 0.14);
    }

    .dm-control-icon {
        width: 46px;
        min-width: 46px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        background: var(--dm-brand-soft);
        color: var(--dm-brand);
        border-right: 1px solid #cde2de;
    }

    .dm-control .form-control,
    .dm-control .form-select {
        border: 0;
        border-radius: 0;
        width: 100%;
        min-width: 0;
        height: 44px;
        box-shadow: none;
    }

    .dm-control.dm-control-select .form-select {
        background-position: right 0.9rem center;
    }

    .dm-actions {
        display: flex;
        justify-content: flex-end;
        gap: 0.5rem;
        flex-wrap: wrap;
    }

    .dm-btn {
        min-width: 114px;
        height: 40px;
        border-radius: 10px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.34rem;
        font-size: 0.84rem;
        font-weight: 700;
        border: 1px solid transparent;
        transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    .dm-btn:hover {
        transform: translateY(-1px);
    }

    .dm-btn:focus-visible {
        outline: none;
        box-shadow: 0 0 0 3px rgba(11, 125, 120, 0.24);
    }

    .dm-btn-primary {
        color: #fff;
        background: var(--dm-brand);
        box-shadow: 0 8px 16px rgba(11, 125, 120, 0.2);
    }

    .dm-btn-primary:hover,
    .dm-btn-primary:focus {
        color: #fff;
    }

    .dm-btn-soft {
        color: #2e515d;
        background: #f7fbfd;
        border-color: #d2e2e8;
    }

    .dm-btn-success {
        color: #fff;
        background: #1a8f58;
        box-shadow: 0 8px 16px rgba(26, 143, 88, 0.2);
    }

    .dm-table-card {
        padding: 1rem;
    }

    .dm-table-head {
        display: flex;
        justify-content: space-between;
        align-items: baseline;
        flex-wrap: wrap;
        gap: 0.8rem;
        margin-bottom: 0.65rem;
    }

    .dm-table-head h2 {
        margin: 0;
        font-size: 1.05rem;
        font-weight: 700;
    }

    .dm-table-hint {
        color: var(--dm-muted);
        font-size: 0.8rem;
    }

    .dm-modern-page table.dataTable thead th {
        background: #f4faf9;
        border-bottom: 1px solid #dce9eb;
        color: #32515c;
        font-size: 0.82rem;
        vertical-align: middle;
    }

    .dm-modern-page .table-hover tbody tr:hover {
        background: #f5fbff;
    }

    .dm-modern-page table.dataTable tbody td {
        padding-top: 0.72rem;
        padding-bottom: 0.72rem;
        border-color: #e1ecef;
        vertical-align: middle;
    }

    .dm-modern-page .dm-row-actions-trigger {
        width: 2.45rem;
        height: 2.45rem;
        border: 1px solid #d2e2e8;
        background: #f7fbfd;
        color: #2f5662;
        transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.15s ease;
    }

    .dm-modern-page .dm-row-actions-trigger:hover {
        transform: translateY(-1px);
        background: #eef7fb;
        color: #204652;
    }

    .dm-modern-page .dm-row-actions-trigger:focus-visible,
    .dm-modern-page .dm-row-actions.show .dm-row-actions-trigger {
        outline: none;
        border-color: #8fc8c4;
        box-shadow: 0 0 0 0.2rem rgba(11, 125, 120, 0.14);
        color: var(--dm-brand);
    }

    .dm-modern-page .dm-row-actions-menu {
        min-width: 220px;
        border: 1px solid #d7e6ea;
        border-radius: 12px;
        box-shadow: 0 14px 28px rgba(16, 40, 46, 0.14);
        padding: 0.35rem;
    }

    .dm-modern-page .dm-row-actions-item {
        border-radius: 8px;
        font-size: 0.84rem;
        font-weight: 600;
        color: #2c4f5a;
        display: flex;
        align-items: center;
        gap: 0.55rem;
        padding: 0.45rem 0.55rem;
    }

    .dm-modern-page .dm-row-actions-item-icon {
        width: 18px;
        text-align: center;
        color: #2e6f7c;
        opacity: 0.95;
    }

    .dm-modern-page .dm-row-actions-item:hover,
    .dm-modern-page .dm-row-actions-item:focus {
        background: #eef7fb;
        color: #1f4652;
    }
</style>

@section Scripts{
    <script type="text/javascript" src="~/Scripts/Admin/Document/manageDocument.js?v=1.0.4"></script>
}
