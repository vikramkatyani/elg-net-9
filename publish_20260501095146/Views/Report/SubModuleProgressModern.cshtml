@{
    ViewBag.Title = "Sub-module Progress";

    //check admin privileges
    var adminRole = ELG.Web.Helper.SessionHelper.UserRole;
    Layout = ELG.Web.Helper.CommonHelper.GetAdminLayoutPage(adminRole);
}

<div class="smp-modern-page">
    <header class="smp-hero">
        <div>
            <nav class="smp-breadcrumb" aria-label="breadcrumb">
                <a href="@Url.Action("InsightsReports", "Report")">Reports &amp; Analytics</a>
                <span>/</span>
                <span>Sub-module Progress</span>
            </nav>
            <h1 class="smp-title">Sub-module Progress</h1>
            <p class="smp-subtitle">Track learner progress at the sub-module level for finer learning insights.</p>
        </div>
        <a href="@Url.Action("InsightsReports", "Report")" class="smp-back-btn">
            <i class="fa fa-arrow-left"></i>
            Back to Reports
        </a>
    </header>

    <section class="smp-filter-card">
        <div class="row g-3 align-items-end">
            <div class="col-lg-3 col-md-6">
                <label class="form-label smp-label" for="txtLearner">User</label>
                <div class="input-group smp-input-group">
                    <span class="input-group-text"><i class="fa fa-search"></i></span>
                    <input type="text" class="form-control" id="txtLearner" placeholder="Name or email">
                </div>
            </div>

            <div class="col-lg-3 col-md-6">
                <label class="form-label smp-label" for="ddlUserStatus">User Status</label>
                <select class="form-select" id="ddlUserStatus">
                    <option value="0">Select All</option>
                    <option value="1" selected>Active</option>
                    <option value="2">In-active</option>
                </select>
            </div>

            <div class="col-lg-3 col-md-6">
                <label class="form-label smp-label" for="ddlLocation">@ELG.Web.Helper.SessionHelper.CompanySettings.strLocationDescription</label>
                <select class="form-select ddl-loc" id="ddlLocation">
                    <option value="0">Select All</option>
                </select>
            </div>

            <div class="col-lg-3 col-md-6">
                <label class="form-label smp-label" for="ddlDepartment">@ELG.Web.Helper.SessionHelper.CompanySettings.strDepartmentDescription</label>
                <select class="form-select ddl-dep" id="ddlDepartment">
                    <option value="0">Select All</option>
                </select>
            </div>
        </div>

        <div class="row g-3 align-items-end mt-1">
            <div class="col-lg-4 col-md-6">
                <label class="form-label smp-label" for="ddlCourse">Course</label>
                <select class="form-select ddl-course" id="ddlCourse">
                    <option value="0">Select All</option>
                </select>
            </div>

            <div class="col-lg-4 col-md-6">
                <label class="form-label smp-label" for="ddlSubModule">Sub-module</label>
                <select class="form-select ddl-course" id="ddlSubModule">
                    <option value="0">Select All</option>
                </select>
            </div>

            <div class="col-lg-4 col-md-6">
                <label class="form-label smp-label" for="ddlAccessStatus">Access Status</label>
                <select class="form-select" id="ddlAccessStatus">
                    <option value="0">Active</option>
                    <option value="1">Suspended</option>
                </select>
            </div>
        </div>

        <div class="row g-3 align-items-end mt-1">
            <div class="col-lg-3 col-md-6">
                <label class="form-label smp-label" for="ddlStatus">Progress Status</label>
                <select class="form-select" id="ddlStatus">
                    <option value="not-started">Not started</option>
                    <option value="incomplete">In-progress</option>
                    <option value="passed">Passed</option>
                    <option value="failed">Failed</option>
                    <option disabled>----------</option>
                    <option value="0" selected>Select All</option>
                    <option value="not-passed">Not Passed</option>
                </select>
            </div>

            <div class="col-lg-3 col-md-6">
                <label class="form-label smp-label" for="txtRptFrom">Completed From</label>
                <input type="text" class="form-control" id="txtRptFrom" placeholder="yyyy-mm-dd">
            </div>

            <div class="col-lg-3 col-md-6">
                <label class="form-label smp-label" for="txtRptTo">Completed To</label>
                <input type="text" class="form-control" id="txtRptTo" placeholder="yyyy-mm-dd">
            </div>

            <div class="col-lg-3 col-md-12">
                <div class="smp-actions-panel">
                    <button id="searchLearnerProgress" class="btn smp-btn smp-btn-primary">
                        <i class="fa fa-search me-1"></i>Search
                    </button>
                    <button id="downloadProgressReport" class="btn smp-btn smp-btn-soft"
                            data-loading-text="Downloading..."
                            data-original-text='<i class="fa fa-download"></i> Download'>
                        <i class="fa fa-download me-1"></i>Download
                    </button>
                    <button id="clearSearchProgressReport" class="btn smp-btn smp-btn-soft">
                        <i class="fa fa-refresh me-1"></i>Clear
                    </button>
                </div>
            </div>
        </div>
    </section>

    <section class="smp-table-card">
        <div class="smp-table-head">
            <h2>Sub-module Results</h2>
            <span class="smp-table-hint">Use filters to focus the report by learner, course, sub-module, and completion timeline.</span>
        </div>

        <div id="message_test_report" class="mb-3"></div>

        <div class="table-responsive" id="test-report-container">
            <table class="table table-hover align-middle" id="learningProgressReport" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>User</th>
                        <th>Email</th>
                        <th>@ELG.Web.Helper.SessionHelper.CompanySettings.strLocationDescription</th>
                        <th>@ELG.Web.Helper.SessionHelper.CompanySettings.strDepartmentDescription</th>
                        <th>Course</th>
                        <th>Sub-module</th>
                        <th>Assigned On</th>
                        <th>Status</th>
                        <th>Last Accessed On</th>
                    </tr>
                </thead>
            </table>
        </div>
    </section>
</div>

<style>
    .smp-modern-page {
        --smp-surface: #ffffff;
        --smp-line: #d9e5ea;
        --smp-text: #12303a;
        --smp-muted: #6a808c;
        --smp-brand: var(--color-primary, #0b7d78);
        --smp-brand-soft: #e9f6f4;
        --smp-shadow: 0 8px 18px rgba(17, 39, 47, 0.06);
        color: var(--smp-text);
    }

    .smp-hero {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 1rem;
        padding: 0.2rem 0.2rem 0.65rem;
        margin-bottom: 0.45rem;
    }

    .smp-breadcrumb {
        display: flex;
        align-items: center;
        gap: 0.45rem;
        margin-bottom: 0.45rem;
        color: var(--smp-muted);
        font-size: 0.88rem;
    }

    .smp-breadcrumb a {
        color: var(--smp-muted);
        text-decoration: none;
    }

    .smp-breadcrumb a:hover {
        color: var(--smp-text);
        text-decoration: underline;
    }

    .smp-title {
        margin: 0;
        font-size: 2.1rem;
        font-weight: 800;
        letter-spacing: 0.2px;
        line-height: 1.15;
    }

    .smp-subtitle {
        margin: 0.3rem 0 0;
        color: #5d6f7a;
        font-size: 1.05rem;
        line-height: 1.35;
    }

    .smp-back-btn {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        border: 1px solid var(--smp-brand);
        color: var(--smp-brand);
        background: #fff;
        border-radius: 10px;
        padding: 0.45rem 0.75rem;
        font-size: 0.85rem;
        font-weight: 700;
        text-decoration: none;
        white-space: nowrap;
    }

    .smp-back-btn:hover {
        background: var(--smp-brand);
        color: #fff;
        text-decoration: none;
    }

    .smp-filter-card,
    .smp-table-card {
        background: var(--smp-surface);
        border: 1px solid var(--smp-line);
        border-radius: 14px;
        box-shadow: var(--smp-shadow);
    }

    .smp-filter-card {
        padding: 1rem 1rem 0.95rem;
        margin-bottom: 0.95rem;
    }

    .smp-label {
        font-size: 0.78rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.45px;
        color: #5f7884;
        margin-bottom: 0.38rem;
        line-height: 1.2;
    }

    .smp-input-group .input-group-text {
        border-color: #d2e3e8;
        background: #f4fbfa;
        color: #446573;
    }

    .smp-modern-page .form-control,
    .smp-modern-page .form-select {
        border-radius: 10px;
        border-color: #d2e3e8;
        box-shadow: none;
        min-height: 2.65rem;
    }

    .smp-modern-page .form-control:focus,
    .smp-modern-page .form-select:focus {
        border-color: #99c9c4;
        box-shadow: 0 0 0 0.16rem rgba(11, 125, 120, 0.12);
    }

    .smp-actions-panel {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 0.5rem;
    }

    .smp-btn {
        min-height: 2.65rem;
        border-radius: 10px;
        font-size: 0.9rem;
        font-weight: 700;
        line-height: 1.2;
    }

    .smp-btn-primary {
        background: var(--smp-brand);
        border-color: var(--smp-brand);
        color: #fff;
    }

    .smp-btn-primary:hover,
    .smp-btn-primary:focus {
        background: var(--smp-brand);
        border-color: var(--smp-brand);
        color: #fff;
        filter: brightness(0.94);
    }

    .smp-btn-soft {
        background: var(--smp-brand-soft);
        border-color: #cde4e2;
        color: #2a545f;
    }

    .smp-btn-soft:hover,
    .smp-btn-soft:focus {
        background: #dff2ef;
        border-color: #bdddd9;
        color: #1d4751;
    }

    .smp-table-card {
        padding: 0.85rem 1rem 1rem;
    }

    .smp-table-head {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 0.7rem;
        padding: 0.15rem 0.1rem 0.7rem;
    }

    .smp-table-head h2 {
        margin: 0;
        font-size: 1.1rem;
        font-weight: 800;
        color: #163641;
        line-height: 1.25;
    }

    .smp-table-hint {
        color: #68808c;
        font-size: 0.83rem;
    }

    #learningProgressReport thead th {
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.45px;
        color: #56717c;
        background: #f7fbfc;
        border-bottom: 1px solid #deebef;
        white-space: nowrap;
    }

    #learningProgressReport tbody td {
        font-size: 0.89rem;
        color: #1f3d47;
        vertical-align: middle;
        white-space: nowrap;
    }

    #learningProgressReport tbody tr:hover {
        background: #fbfefe;
    }

    @@media (max-width: 991px) {
        .smp-hero {
            flex-direction: column;
            align-items: stretch;
        }

        .smp-back-btn {
            align-self: flex-start;
        }

        .smp-table-head {
            flex-direction: column;
            align-items: flex-start;
        }
    }

    @@media (max-width: 767px) {
        .smp-title {
            font-size: 1.6rem;
        }

        .smp-filter-card,
        .smp-table-card {
            border-radius: 12px;
        }

        .smp-actions-panel {
            grid-template-columns: 1fr;
        }

        .smp-btn {
            width: 100%;
        }
    }
</style>

@section Scripts{
    <script type="text/javascript" src="~/Scripts/Admin/Reports/subModuleProgress.js?v=1.5"></script>
}
